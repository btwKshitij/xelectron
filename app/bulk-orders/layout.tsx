import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Bulk Orders & Corporate Procurement | Xelectron",
  description: "Direct B2B procurement, corporate gifting, institutional sales, and bulk discounts from Xelectron.",
};

export default function BulkOrdersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
