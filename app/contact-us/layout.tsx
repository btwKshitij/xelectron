import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Us | Xelectron Official Customer Care & Sales Desk",
  description: "Get in touch with Xelectron customer support, sales team, and corporate headquarters.",
};

export default function ContactUsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
